#pragma once
typedef unsigned long long UINT64;

extern UINT64 (*GetRev[64])(UINT64, UINT64);

extern UINT64 get_rev0(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev1(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev2(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev3(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev4(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev5(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev6(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev7(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev8(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev9(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev10(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev11(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev12(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev13(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev14(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev15(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev16(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev17(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev18(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev19(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev20(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev21(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev22(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev23(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev24(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev25(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev26(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev27(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev28(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev29(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev30(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev31(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev32(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev33(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev34(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev35(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev36(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev37(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev38(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev39(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev40(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev41(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev42(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev43(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev44(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev45(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev46(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev47(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev48(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev49(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev50(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev51(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev52(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev53(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev54(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev55(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev56(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev57(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev58(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev59(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev60(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev61(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev62(UINT64 b_board, UINT64 w_board, UINT64 move);
extern UINT64 get_rev63(UINT64 b_board, UINT64 w_board, UINT64 move);